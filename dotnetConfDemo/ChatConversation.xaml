<?xml version="1.0" encoding="utf-8" ?>
<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
       xmlns:control="clr-namespace:dotnetConfDemo.Controls"
       x:Class="dotnetConfDemo.ChatConversation"
       Title="{Binding UserName}">
    <ShellContent Title="{Binding UserName}">
        <ContentPage>
            <control:ChatConversationControl x:Name="chatControl"></control:ChatConversationControl>
        </ContentPage>
    </ShellContent>
</Shell>